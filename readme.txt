YYSTYPE is type of $$ that is generated by parser
"type of yylval is determined by YYSTYPE"

lex proj.l && yacc proj.y && cc -o proj y.tab.c -ll -Ly && ./proj<test.txt

EXPR '+' EXPR {$$ = $1 + $3; } 
    | EXPR '-' EXPR {$$ = $1 - $3; }
    | EXPR '*' EXPR {$$ = $1 * $3; } 
    | EXPR '/' EXPR {$$ = $1 / $3; } 
    | 